<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://xamarin.com/schemas/2014/forms/design" 
             mc:Ignorable="d" 
             x:Class="CTRLapp.Views.SettingsPages.General.General"
             Title="General">
    <ContentPage.Content>
        <Grid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <!-- Master Password-->
            <StackLayout Grid.Column="0" Style="{StaticResource descriptionEntryStack}">
                <Label Style="{StaticResource descriptionLabel}" 
                       Text="Settings Password: &#10;Without this Password, you won't be able to get into the settings again! You can leave this field blank if you don't want a password &#10;(not implemented yet)" />
                <Grid Grid.Row="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="40" />
                    </Grid.ColumnDefinitions>
                    <Entry Grid.ColumnSpan="2" IsPassword="True" x:Name="settingsPasword" HorizontalOptions="FillAndExpand"/>
                    <ImageButton Grid.Column="1" Source="show_password_icon.png" Pressed="ShowSettingsPassword" BackgroundColor="Transparent" />
                </Grid>

                <Frame HeightRequest="40" BackgroundColor="Transparent"/>

                <StackLayout Orientation="Horizontal">
                    <Label Text="Dark mode   " FontSize="20"/>
                    <Switch x:Name="darkModeSwitch" Toggled="DarkModeSwitchToggled" OnColor="White" ThumbColor="Black"/>
                </StackLayout>
            </StackLayout>

            <!-- Broker Settings-->
            <StackLayout Grid.Column="2">
                <StackLayout Style="{StaticResource descriptionEntryStack}">
                    <Label Text="broker IP: " Style="{StaticResource descriptionLabel}"/>
                    <Entry x:Name="brokerIp" Grid.Row="0"/>
                </StackLayout>
                <StackLayout Style="{StaticResource descriptionEntryStack}">
                    <Label Text="Device Name" Style="{StaticResource descriptionLabel}"/>
                    <Entry x:Name="deviceName"  Grid.Row="1"/>
                </StackLayout>
                <StackLayout Style="{StaticResource descriptionEntryStack}">
                    <Label Text="broker username" Style="{StaticResource descriptionLabel}"/>
                    <Entry x:Name="brokerUsername" Grid.Row="2"/>
                </StackLayout>
                <StackLayout Style="{StaticResource descriptionEntryStack}">
                    <Label Text="broker password" Style="{StaticResource descriptionLabel}"/>
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>
                        <Entry Grid.ColumnSpan="2" x:Name="brokerPassword" IsPassword="True" HorizontalOptions="FillAndExpand"/>
                        <ImageButton Grid.Column="1" Source="show_password_icon.png" Pressed="ShowBrokerPassword" BackgroundColor="Transparent"/>
                    </Grid>
                </StackLayout>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>