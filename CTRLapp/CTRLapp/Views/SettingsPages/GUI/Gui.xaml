<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:d="http://xamarin.com/schemas/2014/forms/design" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d" 
             x:Class="CTRLapp.Views.SettingsPages.GUI.Gui"
             Title="GUI">
    
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <!-- Menu Lists-->
            <StackLayout Grid.Column="0" >
                <Label Text="Main Menu" HorizontalTextAlignment="Center" />
                <Frame BackgroundColor="#20505050" HorizontalOptions="Fill" VerticalOptions="FillAndExpand">
                    <ListView x:Name="master_list" IsPullToRefreshEnabled="False" ItemSelected="MasterListItemSelected">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ImageCell Text="{Binding Name}" ImageSource="{Binding IconPath}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>

                    </ListView>
                </Frame>
                <Button Text="Add new" Pressed="AddMasterMenuItem" VerticalOptions="End" HeightRequest="40" />
            </StackLayout>

            <StackLayout Grid.Column="1"  IsVisible="False" x:Name="bottom_stack">
                <Label Text="Secondary Menu" HorizontalTextAlignment="Center" />
            <Frame BackgroundColor="#20505050" HorizontalOptions="Fill" VerticalOptions="FillAndExpand">
                    <ListView x:Name="bottom_list" IsPullToRefreshEnabled="False" ItemSelected="BottomListItemSelected">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <ImageCell Text="{Binding Name}" ImageSource="{Binding IconPath}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>

                    </ListView>
                </Frame>
                <Button Text="Add new" Pressed="AddBottomMenuItem" VerticalOptions="End" HeightRequest="40" />
            </StackLayout>

            <!--edit stack-->
            <StackLayout x:Name="edit_stack" Grid.Column="2"  Padding="4" IsVisible="false" VerticalOptions="Fill" HorizontalOptions="Fill">
                <Label IsVisible="false" x:Name="edit_type" />

            <Label x:Name="Type_label" HorizontalTextAlignment="Center" />
            <Entry x:Name="name_entry" HorizontalOptions="Fill" HeightRequest="50" Placeholder="Menu_name" Text="{Binding Name}"/>
                
                <StackLayout Orientation="Horizontal" x:Name="icons_stack" HeightRequest="60" >
                    <Label Text="icons select, not added yet" />
                    <!--ICONS still to be added-->
                </StackLayout>
                
                <Button Text="Edit Interface" x:Name="Edit_gui" Clicked="EditGuiClicked" />

            <Button Text="Set background image for this screen" x:Name="Edit_background" Clicked="BackgroundImageSelect"/>


            <Button Text="Delete" x:Name="Delete_button" Pressed="DeleteButtonPressed" BackgroundColor="DarkRed" WidthRequest="120" HeightRequest="40" VerticalOptions="EndAndExpand" HorizontalOptions="Center" />
            </StackLayout>

        </Grid>
    
</ContentPage>